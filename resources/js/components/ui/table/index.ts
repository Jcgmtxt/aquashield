// Exportar todos los componentes de tabla
export { Table } from '../table'
export type { TableColumn, TableAction, TableProps } from '../table'

export { TableFilters } from '../table-filters'
export type { FilterOption, TableFiltersProps } from '../table-filters'

// Exportar el hook
export { useTable } from '../../../hooks/useTable'
export type {
  UseTableOptions,
  TableState,
  TableActions
} from '../../../hooks/useTable'
